import React from "react";

const StarsBackground: React.FC = () => (
  <div
    style={{
      position: "absolute",
      inset: 0,
      zIndex: 0,
      pointerEvents: "none",
      overflow: "hidden",
    }}
  >
  </div>
);

export default StarsBackground;
